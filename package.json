{
  "dependencies": {
    "@tinacms/cli": "^1.9.2",
    "tinacms": "^2.7.2"
  },
  "scripts": {
    "dev": "TINA_PUBLIC_IS_LOCAL=true tinacms dev -c \"next dev\"",
    "build": "tinacms build && next build"
  },
  "devDependencies": {
    "@types/node": "^22.13.8"
  }
}
